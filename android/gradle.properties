org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC -XX:MaxGCPauseMillis=200
android.useAndroidX=true
android.enableJetifier=true
org.gradle.java.home=C\:\\Program Files\\Microsoft\\jdk-17.0.17.10-hotspot

# Performance optimizations
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Android build optimizations
android.enableR8.fullMode=true
# android.enableBuildCache removed - deprecated in AGP 7.0+, use Gradle build cache instead

# Gradle user home and cache - using defaults to avoid cross-drive path issues
# Removed forced C: drive paths to prevent conflicts with D: drive project location
# Gradle will use %USERPROFILE%\.gradle by default

# Suppress deprecation warnings from dependencies
android.suppressUnsupportedCompileSdk=34
org.gradle.warning.mode=none